# Core Flask dependencies
Flask==3.1.2
gunicorn==23.0.0
python-dotenv==1.2.1

# OpenAI and LangChain (lightweight - no local models)
openai==2.7.1
langchain-core==1.0.3
langchain-community==0.4.1
langchain-openai==1.0.2

# FAISS vector store (lightweight, CPU-only)
faiss-cpu==1.12.0
numpy==2.3.4  # Required by faiss-cpu

# OpenAI tokenizer
tiktoken==0.12.0

# HTTP and networking
requests==2.32.5
certifi==2025.10.5
charset-normalizer==3.4.4
idna==3.11
urllib3==2.5.0

# Windows-specific (for Outlook integration)
pywin32==306; sys_platform == "win32"

# Removed heavy dependencies:
# - torch, transformers, sentence-transformers (replaced with OpenAI embeddings)
# - scikit-learn, scipy, joblib (not needed without local ML models)
# - huggingface-hub (not needed without HuggingFace models)
# - langchain-huggingface (replaced with langchain-openai)
